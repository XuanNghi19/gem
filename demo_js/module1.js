const a = 5;
const b = 10;
export {a, b };
export const sum = a + b;

function greet(name) {
  return `Hello, ${name}!`;
};

const newPromise = new Promise((resolve, reject) => {
  
});

newPromise.then().

export default greet;